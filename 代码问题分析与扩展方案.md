# Personal FIT Agent 当前代码审查与后续计划

> 初始审查：2026-07-15；修复状态更新：2026-07-23
>
> 范围：当前 `refactor/tools` 工作树中的 `agent/`、`app/`、`core/`、`fit/`、`sinks/` 与测试。
> 本文是当前快照，不沿用已删除 `core/file_workflow.py`、旧 workflow 或旧 Strava token 行为的历史结论。

## 一、结论

主链路已经完成从旧 workflow 到原生 tool-use 的迁移：Main Agent 处理活动定位、权限与业务工具；`ActivityAnalysisAgent` 独立完成单 FIT 数据读取、分析提交和 summary/history 持久化。`submit_analysis` 已成为子 agent 的明确完成协议，定向问题也会透传到子 agent。

当前测试基线为 **323 passed**。审查确认的 4 个高优先级问题已在当前工作树修复；summary 身份与 dashboard token 状态仍是后续 P2。跑步 MVP 已具备 sport-aware Prompt、配速、步频、可用跑步动态和快速跑段扫描。

## 二、当前架构基线

```text
CLI / Web API
  -> Main Agent (`agent/main_agent/`)
  -> 活动解析、权限与业务 handler
  -> ActivityAnalysisAgent (`agent/activity/analysis_agent.py`)
  -> 只读 FIT 数据工具 + submit_analysis
  -> summary JSON / history JSONL / activity index
  -> 可选 Strava 上传
```

- `agent/main_agent/`：外层原生 tool-use 循环、确认恢复、guard 与主业务工具分发。
- `agent/activity/analysis_agent.py`：单活动子 agent、FIT 数据工具循环、`submit_analysis` 收束和本地持久化。
- `agent/tools/fit_analysis/`：活动概览、完整摘要、区间、分段扫描与历史数据等只读工具。
- `core/`：活动索引、历史、Garmin 下载、Strava 上传和通用工具。
- `sinks/strava.py`：OAuth token 生命周期与 Strava HTTP 客户端。

## 三、审查发现与处理状态

### P1（已修复）：首次 Strava OAuth 认证链路无法启动

**位置：** `sinks/strava.py:38-43`，`app/cli.py:111-122`

`StravaSink()` 在构造时无条件调用 `_access_token()`。而 `strava-auth-url` 和 `strava-exchange-code` 都先构造该对象；首次配置通常只有 `client_id` 与 `client_secret`，尚不存在 access token 或 refresh token，因此在生成授权 URL 之前就抛出异常。

本次用只有 `client_id/client_secret` 的内存配置复现，结果为：

```text
RuntimeError: Please configure strava.access_token or
strava.client_id/client_secret/refresh_token in config.yaml
```

这使 README 中的首次 OAuth 命令无法按顺序使用。现有测试通过预置 access token 创建 `StravaSink`，没有覆盖真正的首次认证场景。

**建议修复：** 让授权 URL 和 code exchange 不依赖 access token。可将 OAuth 元数据操作提取为独立客户端，或给 `StravaSink` 增加 `require_access_token=False` 的显式构造模式；上传、查询活动等资源操作仍必须要求有效 token。

**建议测试：** 仅有 `client_id/client_secret` 时，`strava-auth-url` 能生成 URL，`strava-exchange-code` 能保存模拟返回的 token；无 token 的上传仍应明确失败。

**修复状态（2026-07-23）：** `StravaSink(require_access_token=False)` 允许首次授权与换取 code；资源请求仍在首次使用授权头时检查 token，并新增 CLI 与 sink 回归测试。

---

### P1（已修复）：确认发生在最大轮次边界时，会绕过 Main Agent 的步数上限

**位置：** `agent/main_agent/loop.py:275-285`

暂停时保存的 `step_count` 是已经完成的 LLM 调用数。恢复时使用：

```python
remaining_steps = max(1, max_steps - used_steps)
```

当第 10 次调用正好请求需要确认的工具时，`used_steps == max_steps == 10`，代码仍把剩余额度强制设为 1，确认后会再请求一次 LLM。`_build_result()` 又只检查恢复阶段的局部步数，因此最终会报告 `completed`，而不是全局的 `max_steps_exceeded`。

本次用 mock LLM 复现：前 9 轮普通工具调用、第 10 轮请求下载并暂停；确认后实际发起第 11 次 LLM 调用，结果为 `completed`。

**建议修复：** 将全局已用 LLM 轮次保存在恢复状态中，并在确认工具结果回填后先判断是否仍有预算；没有预算时直接返回明确的步数耗尽结果，不再进入 `agent_loop()`。结果状态也应基于全局轮次计算。

**建议测试：** 新增“第 `MAX_TOOL_STEPS` 轮触发确认”的回归测试，并断言确认后 LLM 调用次数不增加、状态为步数耗尽或定义好的待续状态。

**修复状态（2026-07-23）：** 恢复时不再用 `max(1, ...)` 强行保留额度；预算耗尽会执行已确认的工具后直接返回 `max_steps_exceeded`，不会产生第 11 次 LLM 调用。

---

### P1（已修复）：Web API 将本地文件读取与 Strava 上传能力暴露给未认证请求

**位置：** `app/api.py:176-205`，`app/static/app.js:218-229`

Web API 没有认证或来源限制：

- `/api/fit-files/analyze` 直接将调用方提供的 `path` 交给 FIT 分析；
- `/api/strava/upload` 直接将调用方提供的 `summary_path` 交给上传逻辑；
- 前端上传按钮直接请求上传接口，未显示二次确认。

这不是单纯的“目录遍历”问题，而是未经身份校验的本地能力暴露：可触发昂贵 LLM 调用、读取本地 FIT 路径，或借用已配置的 Strava 身份执行外部副作用。若服务绑定到非 loopback 地址或被反向代理暴露，风险会立即升级。

**建议修复：**

1. 默认仅绑定 `127.0.0.1`；若允许网络访问，强制本地 token / Basic Auth。
2. FIT 分析只接受当前配置的输出目录内、后缀为 `.fit` 的 canonical path。
3. 上传只接受 `data/summaries/` 内、后缀为 `.summary.json` 的 canonical path，并要求服务端访问控制 token。
4. 为 API 新增 `TestClient` 覆盖：未认证、越界路径、正常路径等场景。

**修复状态（2026-07-29）：** API 默认只接受 loopback 客户端；配置 `web_api_token` 后必须携带 `X-API-Token`。分析和上传分别限制在受管 FIT/summary 目录。此前的应用层上传确认已随工作流重构删除，外部访问仍由 API token 和路径边界保护。

---

### P1（已修复）：CLI/API 声称可分析任意 FIT 路径，但项目外路径会在分析完成后失败

**位置：** `agent/activity/analysis_agent.py:139-150`，`core/activity_index.py:248-262`

`analyze_fit_file()` 接受绝对路径；CLI 的 `analyze-file` 也明确允许传入具体 FIT 路径。但结果构造时执行：

```python
path.resolve().relative_to(Path.cwd())
```

项目目录以外的 FIT 文件无法相对当前工作目录表示，会抛出 `ValueError`。该异常发生在 FIT 解析和 LLM 分析之后，意味着用户可能已经消耗了一次分析调用，仍得不到 summary 或报告。

本次以 `/tmp/pfa-review-outside.fit` 和 mock 分析结果复现：

```text
ValueError: '/tmp/pfa-review-outside.fit' is not in the subpath of
'/home/liuhaowen/codes/personal_fit_agent'
```

**建议修复：** 定义统一的路径序列化函数：仓库内文件保存相对路径，仓库外文件保存 canonical absolute path（或复制/导入到受管 FIT 目录后再分析）。`activity_index`、history、summary 与上传入口必须共用该函数。

**修复状态（2026-07-23）：** 新增统一路径序列化；仓库内维持相对路径，CLI 导入的项目外 FIT 保存 canonical absolute path。Web API 仍只允许配置的受管目录。

---

### P2：summary 文件以文件名 stem 作为身份，手动导入时可能相互覆盖

**位置：** `agent/activity/analysis_agent.py:432-435`，`agent/operations.py:161-167`，`core/activity_index.py:258`

summary 的命名形式是 `data/summaries/{fit_path.stem}.summary.json`。来自不同目录、但同名的 `ride.fit` 会共用同一个 summary 文件；之后展示、索引和上传都可能关联到另一条活动。Garmin 当前下载文件名包含 activity ID，正常同步时较少触发，但本地导入、其他数据源和未来多平台接入都会遇到。

**建议修复：** 用稳定 `activity_key`（FIT 内容 hash）或 `source + source_activity_id` 命名 summary；提供旧 stem 文件的兼容读取与一次性迁移。将“FIT → summary path”的计算集中为单个 resolver。

---

### P2：Strava 状态面板没有读取持久化 token store

**位置：** `app/api.py:64-80`，`sinks/strava.py:38-43`

Strava OAuth 成功后，最新 token 写入 `.strava_tokens.json`。但 dashboard 只检查 `config.yaml` 中的 access token 或三元组 `client_id/client_secret/refresh_token`。如果用户按持久化策略只在配置保留 client 信息、token 位于 token store，实际可用的 Strava 会在 UI 显示为“未配置”。

**建议修复：** 抽取无网络的 `StravaSink.is_configured()` / `auth_state()`，同时考虑 config 与 token store，并在 UI 中区分“已配置、token 过期、需要首次授权”。

## 四、已验证的现状与非问题

- 子 agent 已有 `submit_analysis` 完成协议；不再依赖模型自然停止来作为成功信号。
- 定向活动问题会通过 `user_request` 传到 `ActivityAnalysisAgent`；已有通用 summary 时使用只读定向分析，不覆盖缓存报告。
- Strava access/refresh token 已有本地持久化、有效期复用和刷新 token 轮换处理；“每次初始化都刷新 token”不再是当前缺陷。
- README 中的 FIT 工具列表与当前 6 个只读工具一致，包含 `scan_activity_segments`。
- 当前完整测试套件通过：`323 passed`。

## 五、建议实施顺序

### 第一批：已完成的正确性、安全与跑步 MVP

1. 首次 Strava OAuth bootstrap 与 CLI 回归测试。
2. 确认恢复的全局步数预算与最大轮次边界测试。
3. Web API 本机限制、token、受管路径与上传确认。
4. 项目外 FIT 的统一路径序列化。
5. 跑步 sport-aware Prompt、配速/步频/动态数据和快速跑段扫描。

### 第二批：稳定本地数据身份

5. 以 `activity_key` / source ID 重构 summary 命名，处理旧 summary 兼容与迁移。
6. 抽取 Strava 认证状态查询，修正 dashboard 显示。
7. 增加 API、OAuth bootstrap、路径导入和边界恢复的测试；将 pytest 接入 CI。

### 第三批：能力扩展

8. 在 FIT 数据工具层增加功率区间、心率区间、功率曲线、Pw:Hr 解耦和训练负荷聚合等确定性计算。
9. 当活动规模与查询需求确实增长后，再将 JSONL/JSON 索引迁移到 SQLite；不应为了替换文件格式而先行引入数据库。
10. Web 有真实多用户或长任务需求时，再引入后台任务、任务状态与轮询；仅把 endpoint 改为 `async def` 不会让同步 LLM/下载调用自动并发。
11. 基于稳定的数据工具与历史负荷能力，补充真实 Garmin 跑步 FIT fixture，再扩展多数据源和训练计划生成。

## 六、验证记录

```text
python -m pytest -q
323 passed in 2.10s
```

另外完成了两项无外部请求的定向复现：

- 仅配置 `client_id/client_secret` 时构造 `StravaSink` 后生成授权 URL，确认原实现在 token 检查处失败；现已由 OAuth-only 构造模式覆盖。
- 将确认放在 Main Agent 第 10 轮工具调用，确认后原实现会发起第 11 次调用；现已断言不会增加 LLM 调用。
