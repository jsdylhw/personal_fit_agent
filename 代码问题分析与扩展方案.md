# Personal FIT Agent 代码问题分析与扩展方案

## 一,项目现状概述

Personal FIT Agent 是一个本地运动数据 Agent 工具,核心流程为:从 Garmin 中国区下载 FIT 文件 → LLM 多轮工具调用分析 → 生成中文报告/Strava 总结/历史记录 → 上传到 Strava.项目已实现 CLI(11 个命令),FastAPI Web 界面,多轮引导对话分析三个入口.

当前代码量约 2500 行 Python,分布在 `agent/`,`app/`,`core/`,`fit/`,`sinks/` 五个包中.核心流程可跑通,但代码组织有待改善.

---

## 二,架构定位:大模型做分析,本地做数据

### 2.1 先澄清一个关键架构决策

`docs/` 中的两份设计文档(`project-architecture.md`,`activity-analysis-pipeline.md`)是在项目早期编写的,它们假定的架构是:

```
本地解析 FIT → 本地 analysis/ 模块做专业计算 → 把分析结果喂给 LLM → LLM 写报告
```

而现在实际跑通的架构是:

```
本地解析 FIT → 提供数据查询工具 → LLM 通过 tool loop 按需调用工具 → LLM 自己分析并写报告
```

**这两种架构有本质区别.** 前者把"分析推理"放在本地代码,LLM 只负责表达;后者把"分析推理"完全交给 LLM,本地只负责提供高质量的结构化数据.

**后者的优势:**
- 分析逻辑由 LLM 的 System Prompt 控制,调整 Prompt 就能改变分析风格和深度,无需改代码
- LLM 能根据上下文灵活选择分析角度(用户问什么就侧重什么),本地写死的分析流水线做不到
- 新增分析能力只需增加数据工具 + 更新 Prompt,不需要实现复杂的分析算法
- 避免"双轨分析"——本地算一套结论,LLM 再算一套,两者可能矛盾

**后者的代价:**
- 每次分析消耗更多 Token(LLM 需要读取数据,推理,写作)
- 分析质量取决于 LLM 能力和 Prompt 质量
- 确定性的计算(如功率区间占比)如果交给 LLM 估算,可能不精确

**结论:当前架构方向是正确的,不需要照搬设计文档中的 `analysis/` 专业分析层.** 但不意味着不需要改进 —— 需要做的是:优化数据工具的质量和丰富度,把本地擅长的确定性计算做好,把分析推理留给 LLM.

### 2.2 当前架构的实际问题

设计文档中规划的模块,在当前架构下需要重新审视:

| 设计文档规划 | 当前架构下的定位 |
|---|---|
| `analysis/` 的 intensity / segments / physiology / fatigue 等分析模块 | **不需要** —— 这些是分析推理,应由 LLM 完成 |
| `analysis/` 的 power_zones / hr_zones / decoupling 计算 | **需要** —— 这些是确定性数学计算,LLM 不应估算,应作为数据工具提供给 LLM |
| `sources/` 数据来源抽象层 | **需要** —— 与架构无关,纯工程优化 |
| `core/storage.py` SQLite 索引 | **需要** —— JSONL 无法扩展 |
| `agent/tools.py` 稳定工具函数 | **需要** —— 当前工具定义和实现耦合在一个大文件里 |
| `agent/prompts.py` 独立 Prompt | **需要** —— Prompt 是核心资产,应从业务代码中分离 |

### 2.3 `core/file_workflow.py` 职责过重(1163 行)

单个文件包含:LLM 工具循环编排,工具定义目录,工具路由,5 个工具的具体实现,15+ 个 summary section builder,10+ 个数值计算辅助函数,JSON 解析,报告写入,历史记录管理,System Prompt 文本.

**重构方向(与设计文档不同):** 不是把分析逻辑拆到 `analysis/` 模块,而是沿"数据工具"和"编排"两条线拆分:

- `agent/prompts.py` — System Prompt(核心资产,当前混在业务代码里)
- `agent/tools.py` — 工具定义目录 + 路由(LLM 可用的工具清单)
- `core/data_tools.py` — 各工具的具体实现(纯数据提取和确定性计算)
- `core/workflow.py` — tool loop 编排 + 报告写入 + 历史管理(流程控制)
- `core/stats.py` — 数值计算辅助函数(`_numeric_field_stats`,`_series_stats`,`_rows_to_column_arrays` 等)

---

### 2.4 没有测试(严重)

项目没有任何测试文件,`tests/` 目录不存在.对于一个处理外部数据,调用付费 API,操作 Strava 上传的工具,零测试覆盖率是高风险状态.

**建议:** 优先为核心模块添加测试:
- `fit/parser.py` → 用 sample FIT 文件做解析正确性测试
- `core/history.py` → JSONL 读写和查询逻辑测试
- `core/file_workflow.py` → 各 tool 函数的单元测试(无需 LLM)
- `sinks/strava.py` → 用 mock 做 Strava API 交互测试

---

### 2.5 安全风险

| 问题 | 位置 | 风险等级 |
|---|---|---|
| API Key 以明文实例属性存储 | `agent/llm.py:18` | 中 |
| Web UI 无认证/授权 | `app/api.py` | 高 — 任何可访问端口的人都能下载数据,调用 LLM,上传 Strava |
| API 接口接受任意文件路径(目录遍历风险) | `app/api.py:140` `analyze_fit_endpoint` 接受 `path` 参数直接传给 `analyze_fit_file` | 中 |
| `os.environ` 全局代理设置 | `download_garmin_cn_fit.py:114-118` | 低 — 影响整个进程的网络请求 |
| `_extract_json_object` 的裸 `raise` | `core/file_workflow.py:1105` | 低 — JSON 提取失败时抛出 `JSONDecodeError` 无上下文 |

**整改建议:**
- Web UI 至少加入 Basic Auth 或本地 Token 认证
- API 路径参数做路径白名单校验,限制在 `garmin_cn_fit_files/` 和 `data/fit/` 内
- 代理设置改为局部生效,退出登录时恢复

---

### 2.6 代码质量问题

#### 2.6.1 重复代码

**`_read_existing_summary` 完全重复:**
- `core/file_workflow.py:612-619`
- `agent/guided_chat.py:394-401`

**`agent/llm.py` 中的 `build_message_payload` / `build_messages_payload` 方法:**
这两个方法与其对应的 `create_*` 方法几乎相同,但从未被调用.应删除或合并.

**工具结果构建中的重复模式:**
`get_time_intervals_tool` 和 `get_distance_intervals_tool` 有大量重复逻辑(group 迭代,row 构建,series stats 调用),应抽取公共函数.

#### 2.6.2 异常处理粗糙

- 全项目只用 `RuntimeError` 和 `ValueError`,没有自定义异常类
- `_read_existing_summary` 静默吞掉 `JSONDecodeError` 和 `OSError`,返回 `{}`,可能掩盖文件损坏
- `call_fit_analysis_tool` 的 `except Exception as exc` 范围过宽
- `_extract_json_object` 在无法找到 JSON 对象时 `raise` 裸 `JSONDecodeError` 而非带上下文的错误

#### 2.6.3 魔法数字

| 位置 | 魔法值 | 建议 |
|---|---|---|
| `file_workflow.py:138` | `range(1, 9)` 最多 8 轮工具调用 | 提取为 `MAX_TOOL_LOOP_STEPS = 8` |
| `file_workflow.py:358` | `max_records=80, max 300` | 提取为常量 |
| `file_workflow.py:963` | `bucket_seconds` 范围 `1-600` | 提取为 `MIN_BUCKET_SECONDS, MAX_BUCKET_SECONDS` |
| `file_workflow.py:971` | `bucket_distance_m` 允许值列表 | 提取为模块常量 |
| `llm.py:114` | `timeout_seconds` 默认 300 | 统一从 config 读取,去硬编码 |

#### 2.6.4 临时文件未清理

- `1.py` — 调试脚本,应移至 `demos/` 或删除
- `test.json` — 工具列表草稿,应删除
- `demos/` — 空目录,要么放入内容要么删除

#### 2.6.5 无日志系统

全局使用 `print()` 输出调试信息(`download_garmin_cn_fit.py`),没有使用 `logging` 模块.LLM 请求/响应没有结构化日志,排查问题困难.

---

### 2.7 功能缺陷

#### 2.7.1 历史记录不能扩展

`core/history.py` 使用 JSONL 文件,每次查询都要完整读取和解析.几十条记录尚可,几百条时会成为性能瓶颈.设计文档中规划的 SQLite 方案可实现索引查询.

#### 2.7.2 FastAPI 全同步阻塞

`app/api.py` 所有端点都是同步函数.LLM 调用可能需要 30-120 秒,期间整个 FastAPI 事件循环被阻塞,其他请求无法处理.应改为 `async def` + `run_in_executor` 或将 LLM 调用放到后台任务队列.

#### 2.7.3 Strava Token 每次初始化都刷新

`StravaSink.__init__` 在每次创建实例时通过 refresh_token 获取新 access_token,产生不必要的 HTTP 请求.应缓存 access_token 并检查过期时间,只在即将过期时才刷新.

#### 2.7.4 时区处理不可靠

`fit/parser.py:277-282` 的 `_local_iso` 函数:
```python
local_tz = datetime.now().astimezone().tzinfo  # 服务器的本地时区
return dt.astimezone(local_tz).isoformat()
```
这会把活动时间转换为服务器所在时区,而非活动实际发生的时区.FIT 文件中的 `start_time` 是 UTC,应保持 UTC 或根据活动的 GPS 位置推断时区.

#### 2.7.5 FIT 文件无法去重

`download_garmin_cn_fit.py` 的 `existing_fit_paths` 仅按文件名前缀匹配,不做内容 hash.如果同名活动重新下载(如修改后),旧文件会被跳过.

#### 2.7.6 Strava 上传无确认机制

CLI `upload-strava` 命令直接上传,不询问确认.Web UI 的上传按钮也无二次确认.上传是副作用操作,应增加确认步骤.

#### 2.7.7 错误重试不均衡

- LLM 客户端有指数退避+重试(timeout-only)
- Strava 客户端无任何重试机制
- FIT 下载无重试

---

### 2.8 文档与代码不一致

- **README 列出的内部工具有误:** README 的"隐藏分析工具"段落列出了 `get_fit_summary`,`get_laps`,`get_numeric_stats`,`get_sampled_records`,`get_training_metadata`,`get_history`,但实际的 `fit_analysis_tool_catalog()` 只暴露了 5 个工具:`get_activity_overview`,`get_activity_summary`,`get_time_intervals`,`get_distance_intervals`,`get_history`(旧工具仍然存在但不在目录中).
- **设计文档中的模块大部分未实现**(见 2.1 节对照表)

---

## 三,扩展方案

核心原则:**本地负责数据提取 + 确定性计算,LLM 负责分析推理 + 报告写作.** 扩展方向是丰富数据工具,优化 Prompt,提升工程质量,而非在本地实现分析推理模块.

---

### 阶段 1:代码重构 —— 拆分 file_workflow.py(优先级最高)

**目标:** 将 1163 行的单一文件拆分为职责清晰的小模块,不改变功能行为.

```
agent/
  prompts.py          # LLM_FIT_ANALYSIS_SYSTEM_PROMPT 等所有 System Prompt
  tools.py             # fit_analysis_tool_catalog() + call_fit_analysis_tool() 路由

core/
  workflow.py          # analyze_fit_file() + analyze_with_llm() 编排逻辑
  data_tools.py        # 各工具的具体实现(get_activity_overview_tool 等)
  stats.py             # 数值计算辅助函数(_numeric_field_stats, _series_stats 等)
  report.py            # write_brief_report() 等报告生成
```

**预计工作量:** 1-2 天

---

### 阶段 2:丰富数据工具

**目标:** 给 LLM 提供更多高质量的结构化数据,让它的分析更有依据.注意:这些工具只做数据提取和确定性计算,不做分析结论.

新增数据工具:

| 工具 | 做什么(确定性计算) | 不做什么(留给 LLM) |
|---|---|---|
| `get_power_zones` | 根据 FTP 计算各区间的时间/占比 | 不判断"强度是否合理" |
| `get_heart_rate_zones` | 根据阈值心率计算各区间时间/占比 | 不判断"心率响应是否正常" |
| `get_aerobic_decoupling` | 计算前后半程的 Pw:Hr / Pa:Hr 解耦率 | 不判断"疲劳程度" |
| `get_power_curve` | 计算关键时长(5s/30s/1min/5min/20min)的最佳功率 | 不判断"能力水平" |
| `get_smoothed_pace` | 提供配速趋势数据(跑步) | 不判断"配速策略" |
| `get_activity_history_stats` | 返回过去 N 天的训练负荷概要(次数,TSS 范围,运动类型分布) | 不判断"训练趋势" |

这些工具的计算公式是运动科学中的标准公式,应本地精确计算而非让 LLM 估算.

**预计工作量:** 2-3 天

---

### 阶段 3:优化 System Prompt(持续迭代)

**目标:** System Prompt 是当前架构下控制分析质量的"源代码".需要从代码中独立出来,便于迭代和 A/B 测试.

- 将 Prompt 从 Python 模块常量改为独立的 `.md` 或 `.yaml` 文件,支持热加载
- 在 Prompt 中加入更精确的分析指令(如何综合多个工具的数据,如何避免常见分析偏差)
- 增加"数据不足时的降级策略"指令
- 通过 chat_logs 回看实际的 LLM 响应质量,迭代 Prompt

**预计工作量:** 1-2 天 + 持续优化

---

### 阶段 4:SQLite 存储层

**目标:** 替代 JSONL 文件存储,支持索引查询,同步状态追踪.

按 `docs/project-architecture.md` 的设计,建立三张核心表:
- `activities` — 活动索引(source,activity_id,sport_type,start_time,fit_path,fit_sha256,summary_json,analysis_json)
- `strava_sync` — Strava 同步状态(upload_id,strava_activity_id,status,uploaded_at,description_updated_at)
- `agent_memory` — Agent 记忆(训练目标,伤病,偏好,常用路线)

**收益:**
- 历史查询从 O(n) 全文件扫描变为 O(1) 索引查询
- 支持按运动类型,时间范围,训练负荷等多维度过滤
- Strava 上传去重(防止重复上传同一活动)
- Agent 可以记忆用户偏好和目标

**预计工作量:** 2-3 天

---

### 阶段 5:训练负荷追踪(CTL/ATL/TSB)

**目标:** 提供跨活动的聚合数据工具.CTL/ATL/TSB 是数学公式(指数加权移动平均),属于确定性计算,应在本地完成.

新增数据工具:
- `get_training_load_status` — 返回当前 CTL(42天),ATL(7天),TSB
- `get_weekly_summary` — 返回本周训练时长,TSS,运动类型分布

这些工具给 LLM 提供跨活动的聚合数据,LLM 基于这些数据做趋势判断.

**预计工作量:** 2-3 天

---

### 阶段 6:多数据源支持

**目标:** 支持从多个平台导入活动数据,建立统一数据入口.

```
sources/
  __init__.py
  base.py           # SourceProvider 抽象协议
  garmin_cn.py      # Garmin 中国区(已有,需重构)
  garmin_global.py  # Garmin 国际区
  local_folder.py   # 本地 FIT 文件导入
```

统一接口:
```python
class SourceProvider(Protocol):
    def list_activities(self, limit: int) -> list[ActivityRef]: ...
    def download_activity(self, activity_id: str) -> Path: ...
```

**预计工作量:** 2-3 天

---

### 阶段 7:跑步运动支持

**目标:** 当前数据工具以骑行为中心(功率,FTP,踏频),需要扩展跑步相关的数据提取.

纯数据层面的适配:
- FIT 解析器提取跑步特有字段(步频,步幅,GAP,垂直振幅等)
- 新增 `get_running_pace_zones` 数据工具(配速区间)
- 新增 `get_running_dynamics` 数据工具(步频/步幅趋势,触地时间等)
- 工具的输出格式对骑行和跑步保持一致的"列数组"风格

LLM 的 System Prompt 增加跑步分析的指引,分析推理仍由 LLM 完成.

**预计工作量:** 3-4 天

---

### 阶段 8:Web UI 增强

**目标:** 将当前纯静态前端升级为有状态,可交互的管理面板.

- 增加 Basic Auth 登录
- 活动列表增加筛选(运动类型,时间范围,训练负荷)
- 内嵌报告预览(Markdown 渲染)
- 上传 Strava 增加确认对话框
- 显示 Strava 同步状态
- 简单的图表展示(功率分布直方图,心率曲线,配速变化)

**预计工作量:** 3-5 天

---

### 阶段 9:MCP Server 暴露

**目标:** 将数据工具暴露为 MCP Server,供 Claude Desktop 等客户端调用.暴露的是数据查询工具 + 分析编排入口,LLM 通过 MCP 协议调用.

**预计工作量:** 1-2 天

---

### 阶段 10:异步化改造

**目标:** 解决 FastAPI 同步阻塞问题.

- `app/api.py` 端点改为 `async def`
- LLM 调用放到 `run_in_executor` 或后台任务队列
- 长时间分析任务改为提交任务 + 轮询状态

**预计工作量:** 2-3 天

---

### 阶段 11:测试体系

**目标:** 建立 CI 可运行的测试套件.

```
tests/
  fixtures/
    sample.fit                  # 标准测试 FIT 文件
    sample_broken.fit           # 损坏的 FIT 文件
  test_fit_parser.py
  test_history.py
  test_analysis_tools.py
  test_strava_sink.py           # mock Strava API
  test_llm_client.py            # mock LLM API
```

**预计工作量:** 3-5 天

---

### 阶段 12:训练计划生成(LLM 驱动)

**目标:** 给 LLM 提供训练计划所需的数据上下文(历史负荷,可用时间,目标),让 LLM 生成周训练计划.本地只提供数据,不做计划生成逻辑.

**预计工作量:** 2-3 天

---

## 四,优先级排序

```
高优先级(建议立即做):
  1. 拆分 file_workflow.py(Prompt / 工具 / 编排 / 数据工具 四向拆分)
  2. 优化 System Prompt(独立文件,热加载,基于 chat_logs 迭代)
  3. 添加核心模块测试
  4. Web UI 增加认证
  5. API 路径白名单校验

中优先级(近期):
  6. 丰富数据工具(功率区间,心率区间,解耦率,功率曲线,训练负荷)
  7. SQLite 存储层
  8. FastAPI 异步化
  9. 多数据源支持
  10. 代码重复清理和魔法数字常量化

低优先级(中长期):
  11. 跑步运动支持
  12. MCP Server
  13. Web UI 增强(图表,筛选)
  14. 训练计划生成(LLM 驱动)

---

## 五,快速可修复项(低工作量高收益)

以下问题可以在 1-2 小时内修复,建议优先处理:

1. **删除 `1.py`,`test.json`,空 `demos/` 目录**,清理项目根目录
2. **合并 `_read_existing_summary` 的重复实现**,放入 `core/config.py` 或新的 `core/storage.py` 统一导出
3. **删除 `agent/llm.py` 中未使用的 `build_message_payload` / `build_messages_payload` 方法**
4. **提取魔法数字为模块常量**(`MAX_TOOL_LOOP_STEPS`,`MAX_SAMPLED_RECORDS`,`ALLOWED_BUCKET_DISTANCES` 等)
5. **用 `logging` 替换 `print()`**,统一日志输出
6. **修正 README 中的工具列表**,使其与 `fit_analysis_tool_catalog()` 一致
7. **API 路径增加白名单校验**,限制在允许的目录内
8. **Strava Token 增加缓存**,避免每次初始化都刷新
9. **`_extract_json_object` 增加错误上下文**,在无法解析时打印原始文本片段
10. **为 `call_fit_analysis_tool` 增加结构化日志**,记录每次工具调用的名字,参数和耗时
