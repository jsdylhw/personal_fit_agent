一般来说，想做“专业运动分析工具”，不要从 **TSS / IF / Power 单个指标** 入手，而是从这 6 个方向入手：

```text
数据质量 → 基础指标 → 强度分布 → 分段结构 → 生理反应 → 训练建议
```

## 1. 第一优先级：数据质量检查

专业分析第一步不是算指标，而是判断数据能不能信。

要检查：

```text
是否有功率数据
是否有心率数据
是否有 GPS
是否有海拔
是否有踏频 / 步频
是否有暂停
是否有异常尖峰
是否采样不连续
是否 FTP / 最大心率配置可信
```

比如：

```text
功率突然从 180W 跳到 1500W
心率长时间为 0
GPS 轨迹飘移
海拔异常波动
FTP 没设置或明显偏低
```

这些不处理，后面的 TSS、IF、配速、爬升都会失真。

建议先做一个工具：

```python
check_activity_data_quality(activity)
```

输出：

```json
{
  "has_power": true,
  "has_heart_rate": true,
  "has_gps": true,
  "data_gaps": 3,
  "power_spikes": 2,
  "hr_missing_ratio": 0.04,
  "confidence": "medium",
  "warnings": [
    "功率存在少量尖峰，已建议平滑处理",
    "FTP 设置会影响 IF 和 TSS 的可信度"
  ]
}
```

---

## 2. 第二优先级：基础摘要指标

这是用户最容易理解的部分。

骑行常见指标：

```text
时间
距离
均速
爬升
平均功率
标准化功率 NP
最大功率
平均心率
最大心率
平均踏频
消耗
TSS
IF
VI
```

跑步常见指标：

```text
时间
距离
平均配速
最快配速
爬升
平均心率
最大心率
步频
步幅
GAP 坡度调整配速
训练负荷
```

建议工具：

```python
get_activity_summary(activity)
```

但这个工具不要只返回数字，要附带解释。

例如：

```json
{
  "duration_min": 72,
  "distance_km": 31.4,
  "avg_power": 165,
  "normalized_power": 192,
  "if": 0.77,
  "tss": 71,
  "summary": "这是一堂中等偏高负荷的有氧到节奏训练"
}
```

---

## 3. 第三优先级：强度分布

专业感很大一部分来自“这次训练到底练了什么”。

不要只看平均功率，因为平均值会掩盖结构。

要分析：

```text
Z1 恢复区多少时间
Z2 有氧区多少时间
Z3 节奏区多少时间
Z4 阈值区多少时间
Z5 VO2max 区多少时间
Z6 无氧区多少时间
Z7 神经肌肉冲刺区多少时间
```

骑行可以按功率区间，跑步可以按配速区间，通用可以按心率区间。

建议工具：

```python
analyze_intensity_distribution(activity, user_profile)
```

输出类似：

```json
{
  "power_zones": {
    "Z1": "18%",
    "Z2": "46%",
    "Z3": "24%",
    "Z4": "9%",
    "Z5+": "3%"
  },
  "main_stimulus": "有氧耐力 + 节奏耐力",
  "comment": "大部分时间在 Z2-Z3，适合提升基础耐力，但高强度刺激较少"
}
```

这个比单独说 “TSS 71” 专业得多。

---

## 4. 第四优先级：分段结构识别

这一步会让分析从“看总数”变成“看过程”。

你要识别：

```text
热身段
主训练段
恢复段
爬坡段
冲刺段
间歇段
放松段
暂停段
异常段
```

例如骑行：

```text
前 15 分钟：热身
15-45 分钟：稳定 Z2
45-58 分钟：爬坡，功率进入 Z3-Z4
58-65 分钟：恢复
65-70 分钟：两次短冲刺
```

建议工具：

```python
detect_workout_segments(activity)
```

这类工具非常重要，因为用户问“这次练得怎么样”，真正要回答的是：

```text
你是在稳定骑，还是乱骑？
你有没有完成目标强度？
后半程有没有崩？
高强度是不是太早出现？
恢复段够不够？
```

---

## 5. 第五优先级：疲劳、漂移、稳定性

这是专业分析里非常关键的一块。

可以做这些分析：

### 心率漂移 / 有氧解耦

同样功率下，后半程心率明显更高，说明可能有：

```text
疲劳
缺水
温度影响
有氧能力不足
恢复不足
补给不足
```

工具：

```python
analyze_hr_power_decoupling(activity)
```

### 功率衰减

看后半程功率是否掉得明显。

```python
analyze_power_fade(activity)
```

### 配速稳定性

跑步尤其重要。

```python
analyze_pacing_stability(activity)
```

### 踏频变化

骑行可以看疲劳后踏频是否下降。

```python
analyze_cadence_trend(activity)
```

这一类分析能让 Agent 说出更像教练的话：

```text
你前半程功率比较稳定，但 50 分钟后同功率下心率开始上升，说明有一定疲劳积累。这个现象如果经常出现，可能需要加强 Z2 基础耐力，或者检查补水补糖。
```

---

## 6. 第六优先级：历史对比

单次活动很难判断进步，必须和历史比。

可以比较：

```text
最近 7 天训练负荷
最近 4 周平均 TSS
同路线历史最好表现
同类型训练的平均功率
心率-功率关系是否改善
Z2 功率是否提高
FTP 附近持续时间是否增加
长距离后半程是否更稳
```

建议工具：

```python
compare_with_history(activity, history)
```

输出：

```json
{
  "similar_activities_count": 8,
  "avg_power_vs_recent": "+6%",
  "hr_at_same_power": "-4 bpm",
  "endurance_trend": "improving",
  "comment": "相比最近类似骑行，同功率下心率更低，说明有氧效率可能在改善"
}
```

没有历史对比，Agent 很容易空泛地说“表现不错”。

---

## 7. 第七优先级：训练目标匹配

专业分析不能只说数据，还要问：

```text
这次训练是否符合目标？
```

目标不同，评价完全不同。

比如同样一场骑行：

```text
目标是恢复：IF 0.77 太高
目标是 Z2 有氧：Z3/Z4 时间太多
目标是 FTP：阈值时间不够
目标是减脂：时长可以，但强度可能偏高
目标是比赛模拟：强度变化可能合理
```

建议工具：

```python
evaluate_goal_match(activity, user_goal)
```

输出：

```json
{
  "goal": "base_endurance",
  "match_score": 72,
  "matched": [
    "总时长足够",
    "Z2 时间占比较高"
  ],
  "not_matched": [
    "Z3 以上时间略多",
    "后半程心率漂移明显"
  ]
}
```

---

## 8. 最小专业工具集

你可以先不用做太多，先做这 8 个：

```text
1. check_activity_data_quality
2. get_activity_summary
3. analyze_intensity_distribution
4. detect_workout_segments
5. analyze_hr_power_decoupling
6. analyze_pacing_or_power_stability
7. compare_with_history
8. evaluate_goal_match
```

这 8 个比一堆 `request_xxx` 更有价值。

---

## 推荐的专业分析流程

你可以让 Agent 每次按这个流程跑：

```text
1. 读取活动数据
2. 检查数据质量
3. 生成基础摘要
4. 判断运动类型
5. 分析强度分布
6. 识别训练结构
7. 分析疲劳和稳定性
8. 和历史活动对比
9. 判断是否符合训练目标
10. 输出训练建议
```

对应工具链：

```text
parse_fit_file
↓
check_activity_data_quality
↓
get_activity_summary
↓
analyze_intensity_distribution
↓
detect_workout_segments
↓
analyze_fatigue_and_decoupling
↓
compare_with_history
↓
evaluate_goal_match
↓
generate_training_recommendation
```

---

## 最关键的设计思路

专业工具不是“多算几个指标”，而是让系统回答这几个问题：

```text
这次数据可信吗？
这次主要练了什么？
强度结构是否合理？
过程是否稳定？
有没有疲劳或异常？
和以前比有没有进步？
是否符合用户目标？
下一次应该怎么练？
```

所以你应该优先从这几个分析模块入手，而不是继续增加：

```text
request_np
request_avg_hr
request_cadence
request_speed
```

这些只是“查数工具”，不是“分析工具”。

一句话总结：

```text
专业运动分析 = 数据质量 + 强度结构 + 分段过程 + 生理反应 + 历史对比 + 目标匹配
```

先把这套框架做出来，你的 Agent 立刻会比只会调用 Power / TSS / IF 专业很多。
